module.exports = {
    name: "send",
    description: "send",
    async execute(message, args) {
        const Discord = require(`discord.js`);
        const { bot_info, color } = require(`./../config.json`);

        
    }
}